import 'package:flutter/material.dart';

class AppColors {
  // Fondo primera pantalla
  static const Color splashBackground = Color(0xFF1E1E1E);
  static const Color background = Color(0xFFF5F5F5);

  // Colores principales
  static const Color primary = Color(0xFF0088FF);
  static const Color secondary = Color(0xFFFFFFFF);
  static const Color buttonSecondary = Color(0xFFE9E9E9);

  // Colores de texto
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF323030);
  static const Color textHint = Color(0xFF757575);

  // Colores de acento
  static const Color error = Color(0xFFe33629);
}
